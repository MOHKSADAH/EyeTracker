services:
  - type: web
    name: eyetracker
    runtime: docker
    plan: free
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: VIDEO_FRAME_STRIDE
        value: "12"
      - key: VIDEO_MAX_WIDTH
        value: "360"
